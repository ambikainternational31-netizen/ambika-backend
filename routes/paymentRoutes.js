const express = require("express");
const router = express.Router();
const { protect } = require("../middleware/auth");

// Routes will be added as needed for future payment methods

module.exports = router;
